const hostButton = document.getElementById('hostButton');
const hostingURL = "joaco619.github.io"; // Reemplaza "URL_DEL_HOST" con la URL real

hostButton.addEventListener('click', () => {
    window.location.href = hostingURL;
});
